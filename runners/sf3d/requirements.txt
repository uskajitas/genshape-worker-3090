# Stable Fast 3D runtime deps. SF3D needs gsplat + slangtorch which are
# fiddly to install on Windows — recommend installing pre-built wheels if
# the source build fails.
#
# Setup:
#   py -3.11 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install --index-url https://download.pytorch.org/whl/cu121 \
#       torch==2.3.0 torchvision==0.18.0
#   pip install -r requirements.txt

# SF3D not on PyPI — install from GitHub.
git+https://github.com/Stability-AI/stable-fast-3d.git@main#egg=sf3d

# Required by sf3d
gsplat>=1.0
slangtorch
trimesh>=4.0
xatlas>=0.0.9
Pillow>=10.0
numpy>=1.26
huggingface-hub[cli]>=0.20
